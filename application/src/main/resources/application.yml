spring:
  datasource:
    url: jdbc:mysql://localhost:3306/terte_db
    username: test_user
    password: test
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: popular-menu-group
redis:
  host: localhost
  port: 6379